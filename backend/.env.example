# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Google Sheets Configuration
# Add your Google Sheet IDs (comma-separated for multiple sheets)
GOOGLE_SHEET_IDS=your_sheet_id_1,your_sheet_id_2

# If your data is in a specific worksheet/tab (not the first sheet):
# Get the GID from URL after #gid= (e.g., #gid=1186874097)
# Leave empty to use the first sheet
GOOGLE_SHEET_GID=

# Get credentials from: https://console.cloud.google.com/
# 1. Create a project
# 2. Enable Google Sheets API
# 3. Create Service Account
# 4. Download JSON credentials
GOOGLE_CREDENTIALS_FILE=credentials.json

# Server Configuration
PORT=8000
HOST=0.0.0.0

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173
